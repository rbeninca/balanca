{
  "name": "balancagfig",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "pacotes/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "testar:cobertura": "npm run testar:cobertura --workspaces --if-present",
    "testar:ci": "npm run testar:ci --workspaces --if-present",
    "verificar": "npm run verificar --workspaces --if-present",
    "compilar": "npm run compilar --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
