node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.env.local
coverage/
test-results.xml

# Editores — configuração de quem trabalha, não do projeto
.vscode/
.idea/

# Estado local do wrangler (banco D1 de desenvolvimento, cache) e segredos
pacotes/painel/.wrangler/
.dev.vars

# Artefatos de build do PlatformIO
firmware/.pio/

# Sessões do compilador Kotlin (criadas pelo plugin do Gradle a cada build)
android/.kotlin/

# Estado que o box.sh guarda por box — log de trabalho, não do projeto
android/chaves/box/

# Copiados pelo CI a partir de firmware/firmware.bin — não rastrear
pacotes/aplicacao/public/firmware.bin
pacotes/aplicacao/public/firmware-versao.json
balancaGFIGv1/
dist-web/
