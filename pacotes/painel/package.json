{
  "name": "@balancagfig/painel",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "testar:cobertura": "vitest run --coverage",
    "testar:ci": "vitest run --reporter=junit --outputFile=test-results.xml",
    "compilar": "tsc --noEmit",
    "dev": "npx --yes wrangler@4 dev",
    "publicar": "npx --yes wrangler@4 deploy"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "better-sqlite3": "^11.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
