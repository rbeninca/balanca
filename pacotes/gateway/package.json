{
  "name": "@balancagfig/gateway",
  "version": "2.0.0",
  "type": "module",
  "main": "./dist/principal.js",
  "scripts": {
    "compilar": "tsc",
    "iniciar": "node dist/principal.js",
    "desenvolver": "node --watch dist/principal.js",
    "test": "vitest run",
    "testar:cobertura": "vitest run --coverage",
    "testar:ci": "vitest run --reporter=junit --outputFile=test-results.xml"
  },
  "dependencies": {
    "@balancagfig/protocolo": "*",
    "@balancagfig/processamento": "*",
    "serialport": "^12.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
